name: insightops-ai
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - fastapi>=0.110
  - uvicorn
  - pandas
  - numpy
  - python-dotenv
  - requests
  - pydantic>=2.6
  - httpx
  # Optional placeholders for future orchestration/scheduling:
  # - langgraph
  # - apscheduler
